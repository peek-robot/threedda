from setuptools import setup, find_packages

setup(
    name="robotic",
    version="0.1.0",
    description="Robotic learning project with 3D diffusion models",
    packages=find_packages(),
    include_package_data=True,
    python_requires=">=3.8",
)
